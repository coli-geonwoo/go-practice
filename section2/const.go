package main

// 상수1
func main() {

	//const로 사용 초기화, 한번 선언 후 값 변경 금지, 고정된 값 관리용
	const a string = "Test1"
	const b = "Test2"
	const c int32 = 10 * 10
	//const d = getHeight() <- 항상 같은 값을 반환한다는 보장이 없어 함수로 초기화 불가
	const e = 35.6
	const f = false

	//선언과 동시에 초기화해야 함
}
